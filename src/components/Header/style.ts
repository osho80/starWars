import { createStyles } from "@material-ui/core/styles";

const style = () =>
  createStyles({
    header: {
      padding: "10px",
    },
    logo: {
      width: "200px",
    },
  });

export default style;
