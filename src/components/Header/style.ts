import { createStyles, Theme } from "@material-ui/core/styles";

const style = (theme: Theme) =>
  createStyles({
    header: {
      padding: "10px",
    },
    logo: {
      width: "200px",
    },
  });

export default style;
